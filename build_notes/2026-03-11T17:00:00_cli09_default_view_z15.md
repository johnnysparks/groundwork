# Build Notes: CLI-09 — Default view Z=15

**Date:** 2026-03-11T17:00:00
**Task:** CLI-09 (P1)
**Branch:** claude/improve-cli-first-impression-gUl4X

## Change

One-line change in `crates/groundwork-tui/src/cli.rs:109`:
- Before: `.unwrap_or(GROUND_LEVEL + 1)` (Z=16, one above surface — mostly empty air)
- After: `.unwrap_or(GROUND_LEVEL)` (Z=15, surface — where seeds, soil, and water spring live)

## Why

2/6 player sessions flagged that `view` with no arguments shows Z=16 which is mostly empty air with a small pond. The weekend gardener's first command showed nothing interesting. Z=15 is where seeds are placed and where the action starts.

## Validation

- `cargo test -p groundwork-sim` — 23/23 pass
- `cargo check --workspace` — clean
- `view` now defaults to Z=15 (surface) showing soil, water spring, and any placed seeds
