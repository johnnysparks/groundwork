# Dev → Manager Handoff: CLI-09 Default View Z=15

**Date:** 2026-03-11T17:00:00
**Task:** CLI-09 (P1)
**Status:** Complete

## What shipped

`view` command now defaults to Z=15 (surface) instead of Z=16 (empty air above surface). One-line change in `cli.rs`. All 23 tests pass, workspace compiles clean.

## Impact

New players running `groundwork view` as their first command now see the surface with soil, water spring, and any placed seeds — instead of a mostly-empty air layer. This directly addresses the "where's the garden?" first impression flagged by 2/6 player sessions.

## Files changed

- `crates/groundwork-tui/src/cli.rs` — line 109: default Z from `GROUND_LEVEL + 1` to `GROUND_LEVEL`

## Remaining P1 quick wins from the handoff

- SIM-05: Lower wet-soil threshold (2 lines)
- VIS-01: Dark air indicator (2 lines)

These are independent of CLI-09 and can be done in a follow-up.
