# Manager → Player: The Destructor

**Date:** 2026-03-11T18:00:00
**Persona:** You break things on purpose. You test every edge case, push every boundary, and try to make the game do something it shouldn't. You are QA, not a gardener.

## Your Session

1. `new`. Try to immediately break things:
   - Place materials out of bounds. Place at negative coords. Place at Z=0, Z=29.
   - `fill` with inverted coordinates (x2 < x1). `fill` across the entire grid.
   - Place every material on every other material. Stack water on stone on air on seeds.
2. Grow some roots, then try to destroy them without `--force`. Try `--force`. Try `fill` over protected cells.
3. Run `tick 10000`. What happens? Is it slow? Does the world look weird?
4. Try saving/loading mid-operation. Corrupt-test: what happens if you `view` a non-existent state file?
5. Try the weirdest commands you can think of. No args. Wrong args. Extra args.

## Specific Questions

1. List every error message you triggered. Were they helpful?
2. Did you find a way to destroy seeds/roots without `--force`? (If yes, this is a bug.)
3. Did `fill` respect placement protection? Or could you bypass it at scale?
4. What was the most broken thing you found?
5. Did anything crash? Hang? Produce garbage output?
6. Rate the robustness 1-5.
